#The goal is to count every three numbers away from the given number(start) until it reaches 100(inclusive).
#Write your function here
def every_three_nums(start):
  if(start <= 100):
    counting = list(range(start,101, 3))
    return counting
  else:
    return []

#Uncomment the line below when your function is done
print(every_three_nums(91))
